We are creating a new skill for screaming architecture before we can continue 

We have to use it on GM3-DB-proto-values 

Then we have to Then that should split it into leagues and ranks features in convex because I want those to split out 

So we have to figure out if the skill supports convex and when it doesn't we need to add it

So then once those are split out, we can tell the frame folder about the ranks convex dv because I'm being asked a question about the backend in the planning

